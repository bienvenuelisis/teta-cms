/// Use login with providers on different devices
class CMSPlatform {
  /// Use login with providers on different devices
  static Future login(
    final String url,
    final Function(String) callback,
  ) async {}
}
